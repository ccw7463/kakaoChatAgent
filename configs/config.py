from ml_collections import ConfigDict

prompt_config = ConfigDict()

#### 시스템 프롬프트
prompt_config.system_message = """당신은 카카오톡 채널에서 대화하는 챗봇 '미네르바'입니다.

출력 규칙:
- Markdown 이나 텍스트 스타일(**, *, _)을 쓰지 마세요. 카카오톡에서는 기호가 그대로 노출됩니다.
- 700자 이내로 핵심만 답하세요.
- 항목이 여러 개면 '- ' 로 시작하는 목록으로 정리하세요.
- 사용자의 이름을 모르면 지어내지 말고 호칭 없이 존댓말로 답하세요."""

#### 라우팅 프롬프트 (YES/NO 또는 검색어만 출력)
prompt_config.decide_search_prompt = """사용자 요청에 답하려면 웹에서 최신 자료를 찾아야 하는지 판단하세요.

- 검색이 필요 없으면 NO 만 출력하세요.
- 검색이 필요하면 한국어 검색어 한 줄만 출력하세요. 말머리를 붙이지 마세요.
- 오늘 날짜는 {today} 입니다. 최신 정보가 필요하면 반영하세요.

앞의 메시지들은 맥락 파악용입니다. 반드시 [현재 요청 메시지] 를 기준으로 판단하세요.

직전 검색어(참고용): {previous_search_keyword}"""

prompt_config.decide_personal_prompt = """사용자가 자기 자신에 대한 사실을 직접 말했는지 판단하세요.
(이름, 사는 곳, 관심사, 취미, 직업, 전공, 가족관계 등)

추측하지 말고 명시적으로 언급된 것만 봅니다.

앞의 메시지들은 맥락 파악용입니다. 반드시 [현재 요청 메시지] 를 기준으로 판단하세요.
YES 또는 NO 만 출력하세요."""

prompt_config.decide_preference_prompt = """사용자가 답변 방식에 대한 선호를 드러냈는지 판단하세요.
(분량, 말투, 이모지 사용 여부, 예시나 단계별 설명 요구 등)

답변 형식에 대한 요구가 없으면 NO 입니다.

앞의 메시지들은 맥락 파악용입니다. 반드시 [현재 요청 메시지] 를 기준으로 판단하세요.
YES 또는 NO 만 출력하세요."""

#### 저장 프롬프트 (갱신된 전문을 그대로 출력)
prompt_config.create_memory_prompt = """사용자에 대해 알고 있는 정보를 갱신하세요.

[현재 정보]
{memory}

규칙:
- 사용자가 직접 말한 사실만 반영합니다. 추측하지 마세요.
- 새 정보는 기존 정보와 병합하고, 충돌하면 최신 것을 남깁니다.
- 새로 얻은 정보가 없으면 [현재 정보]를 그대로 출력합니다. 비어 있으면 비운 채로 두세요.
- 설명 없이 아래 형식만 출력합니다.

- 항목 : 내용"""

prompt_config.create_preference_prompt = """사용자가 선호하는 답변 방식을 갱신하세요.

[현재 선호도]
{preference}

규칙:
- 답변의 형식과 말투에 대한 선호만 기록합니다.
  (분량, 문체, 이모지 사용 여부, 예시나 단계별 설명 요구 등)
- 이름, 거주지, 직업 같은 사용자의 신상 정보는 선호도가 아닙니다. 넣지 마세요.
- 대화에서 실제로 드러난 선호만 반영합니다. 추측하지 마세요.
- 새 정보는 기존과 병합하고, 충돌하면 최신 것을 남깁니다.
- 새로 얻은 선호가 없으면 [현재 선호도]를 그대로 출력합니다. 비어 있으면 비운 채로 두세요.
- 설명 없이 아래 형식만 출력합니다.

- 사용자는 ~ 를 선호한다."""

#### 답변 프롬프트
# 개인화 블록은 내용이 있을 때만 붙인다. (빈 헤더가 들어가면 토큰만 낭비된다)
prompt_config.memory_block = """[사용자 정보]
{memory}"""

prompt_config.preference_block = """[사용자 선호도]
{preference}"""

prompt_config.recalled_block = """[이전에 나눈 관련 대화]
{recalled}

참고용입니다. 사용자가 묻지 않으면 굳이 언급하지 말고, 현재 요청과 무관하면 무시하세요."""

prompt_config.answer_with_context = """아래 [참고내용]을 바탕으로 [요청]에 답하세요.
참고내용에 없는 사실은 지어내지 마세요.

[요청]
{query}

[참고내용]
{context}"""
